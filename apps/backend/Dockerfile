# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @grocery-shopping/backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t grocery-shopping-backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system grocery-shopping-backend && \
          adduser --system -G grocery-shopping-backend grocery-shopping-backend

COPY apps/backend/dist grocery-shopping-backend/
COPY apps/backend/package.json grocery-shopping-backend/
RUN chown -R grocery-shopping-backend:grocery-shopping-backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix grocery-shopping-backend --omit=dev -f install

CMD [ "node", "grocery-shopping-backend" ]
