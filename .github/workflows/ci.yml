name: Pull request check
on:
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run unit tests
        run: npx nx run-many --target=test --all --parallel

      - name: Start backend server
        env:
          PORT: 3001
          DATABASE_URL: mongodb://localhost:27017/grocery
        run: |
          node apps/backend/dist/main.js > backend.log 2>&1 &

      - name: Wait for backend to be ready
        run: |
          echo "Waiting for backend to start..."
          for i in {1..30}; do
            status=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/products)
            echo "Attempt $i - Status: $status"
            if [ "$status" -eq 200 ]; then
              echo "Backend is ready!"
              exit 0
            fi
            sleep 2
          done
          echo "Backend did not start in time or /products not returning 200."
          echo "-------- backend.log --------"
          cat backend.log
          exit 1

      - name: Run end-to-end tests
        run: nx run @grocery-shopping/frontend-e2e:e2e
